import {
  "moonbitlang/core/encoding/utf8",
}

import {
  "moonbitlang/async",
  "moonbitlang/async/fs",
  "moonbitlang/async/process",
} for "test"

options(
  "native-stub": [ "native.c" ],
  targets: { "capture.mbt": [ "native" ], "capture_test.mbt": [ "native" ] },
)
