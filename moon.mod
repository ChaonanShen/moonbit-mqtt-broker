name = "ChaonanShen/moonbit-mqtt-broker"

version = "0.3.0"

readme = "README.md"

repository = "https://github.com/ChaonanShen/moonbit-mqtt-broker"

license = "Apache-2.0"

keywords = [ "moonbit", "mqtt", "broker" ]

preferred_target = "native"

source = "src"

description = "A lightweight MQTT 3.1.1 and opt-in MQTT 5 broker implemented in MoonBit."

import {
  "moonbitlang/async@0.20.6",
  "bobzhang/toml@0.4.3",
}
